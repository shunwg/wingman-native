/** Spacing and radius scale. 8px base rhythm, matching the web app's --sp-* tokens. */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 28,
  xxxl: 40,
} as const;

export const radius = {
  sm: 12,
  md: 16,
  photo: 18,
  pill: 999,
} as const;
